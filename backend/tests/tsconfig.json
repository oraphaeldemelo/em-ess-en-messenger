{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "types": ["node", "jest"],
        "rootDir": ".."
    },
    "include": ["../src/**/*.ts", "./**/*.ts"],
    "exclude": ["../node_modules", "../dist"]
}